// Placeholder types for the Foundation phase.
// Project, Output, and Transcript interfaces are added in a later phase.

export interface User {
  id: number;
  email: string;
  full_name: string | null;
}
